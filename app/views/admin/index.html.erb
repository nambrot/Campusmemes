<%=  @colleges.map{|d| {:name => d.name, :short => d.short, :url => d.url, :fbid => d.fbid}}.to_json  %>

Photos:
<%= @colleges.map{|d| d.photos.count}.to_json %>
Jobs:
<%= Delayed::Job.all.count %>

<%= form_for @college, :url => admin_addcollege_path, :html => {:class => "nifty_form"} do |f| %>
  Name: <%= f.text_field :name %>
  Short: <%= f.text_field :short %>
  URL: <%= f.text_field :url %>
  FBID: <%= f.text_field :fbid %>
  <%= f.submit "Create" %>
<% end %>